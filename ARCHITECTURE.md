src/
├── components/
│   ├── common/         # componentes reutilizables (botones, inputs)
│   ├── forms/          # selects, inputs y formularios
│   └── layout/         # contenedores, headers, footers
├── screens/            # pantallas migradas desde /screens
├── navigation/         # navegadores reubicados
├── services/           # consumos de APIs / persistencia
├── hooks/              # hooks personalizados
├── context/            # providers y contextos
├── utils/              # helpers y validadores
├── constants/          # constantes y enums
├── types/              # definiciones de TypeScript (si se añaden)
├── assets/             # imágenes y fuentes
└── styles/             # tokens de estilos y themes
