import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  safeArea: { flex: 1, backgroundColor: '#EEF0FF' },
  scrollView: { flex: 1 },
  container: { flexGrow: 1, justifyContent: 'center', alignItems: 'center', padding: 24, paddingBottom: 120 },
  title: { fontSize: 24, fontWeight: 'bold', marginBottom: 20 },
});

export default styles;
